<p style="color: green"><%= notice %></p>

<% if(current_user) then %>
<div class="w-full  py-10" id="friends">
<h2 class="text-3xl">Friends</h2>
  <div class="border-2 border-indigo-600 rounded">
  <% @friends.each do |friend| %>
    <%= render friend  %>
  <% end %>
  </div>
</div>

<%= link_to "New friend", new_friend_path, class: "btn btn-blue" %>

<% else %>

<% end %>

<style>

</style>